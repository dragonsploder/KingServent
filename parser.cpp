#include <string>
#include <iostream>

#include "KingServent.h"

using namespace std;

string getInput(){
    string rawInput;
    int command = 0;
    do {
        cin >> rawInput;
    } while (command == 0);
}